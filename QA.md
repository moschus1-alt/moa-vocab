# 검수 결과 — 2026-09-22

## 직접 확인한 흐름

- 실제 네이버 응답: es `casa`, `hablo`; ja `食べる`; zh `你好`; en `apple`, `went`; 존재하지 않는 단어의 빈 결과.
- 공개 배포 서버에서도 네 언어의 검색이 HTTP 200으로 실제 뜻/읽기/병음/예문을 반환.
- Chrome 모바일 390×844 화면에서 검색 → 뜻 2개 선택 → 저장 → 새로고침 후 보존.
- 동일 단어의 다른 뜻 추가 시 총 단어 수 1개 유지, 뜻 병합 확인.
- 뜻 수정, 단어 삭제 확인.
- `went`를 검색하고 표제어 `go`를 선택하여 별도 검색어와 발음기호 저장 확인.
- 2~4번 범위 학습, 카드 앞뒤 전환, 몰라요/헷갈려요/알아요 평가 확인.
- 몰라요/헷갈려요 카드가 다시 나타나고 총 5회 평가로 완료되는 것 확인.
- 랜덤 학습 시작, 학습 후 복습 가능 단어 수 변경 확인.
- JSON 다운로드 파일이 실제로 생성되고 4개 단어와 학습 기록을 포함하는 것 확인.
- 실제 브라우저의 별도 검증 화면에서 JSON 직렬화 → 삭제 → 복원, 중복 복원, 잘못된 백업의 전체 거절과 원래 데이터 보존을 검증.
- 서버를 종료하고 새 탭에서 단어장을 열어 IndexedDB의 복원 단어·학습 기록 유지 및 오프라인 학습 시작 확인.
- 서비스 워커 HTML/모듈 캐시, 192/512 PNG 아이콘, PWA 설치 안내 및 설치 이벤트 확인.
- 공개 URL에서 다시 검색 → 저장 → 재실행 → 카드학습 → 알아요 → 완료 확인. 이 흐름에서 브라우저 console error 없음.
- 실제 네이버 응답으로 es `tener ganas de`, ja `気になる`, en `look after`를 검색하고 숙어 항목 표시를 확인.
- 단어장의 발음 듣기 버튼에서 es/ja/zh/en 네 언어가 `준비 중 → 재생 중 → 종료`로 전환되는 것을 실제 Chrome 음성 엔진으로 확인. 재생 중 버튼을 다시 누르거나 다른 단어를 누를 때 취소되는 동작도 검사.
- 학습 언어 `스페인어`, 학습 방식 `해석 입력 테스트`, 학습 순서 `랜덤`, 문제 방향 `양방향 섞기`를 고른 뒤 새로고침해 네 설정이 유지되는 것을 확인.
- `casa → 집`과 `집, 가옥, 주택 → CASA`를 입력해 양방향 정답 처리, 오답의 정답 공개 및 뒤쪽 재출제를 실제 브라우저에서 확인.

## 자동 검사

- `npm test`: 총 16건 통과. 복습 간격, 범위/랜덤/복습 필터, 백업 검증, 중복 병합, 숙어 결과 병합, 음성 언어·취소, 학습 설정 저장, 양방향 채점을 검사.
- `npm run test:live`: 실제 네이버 검색 10건 통과. 세 언어의 숙어 검색을 포함하며 사전 데이터 스텁을 사용하지 않음.
- 별도 브라우저 검증 15개 항목 통과.
- `npm run build`: 정적 PWA와 Worker 산출물 생성 성공.

## 검증 제한

- Chrome 확장의 파일 URL 접근 권한으로 자동 파일 선택이 차단되어, **파일 선택기를 통한 JSON 복원 전체 흐름은 미확인**입니다. 같은 저장/복원 함수의 실제 IndexedDB 동작은 브라우저 검증 화면에서 확인했습니다.
- 실제 iPhone/Android 기기의 홈 화면 설치는 미확인입니다. Chrome의 모바일 크기, 설치 가능 이벤트, 매니페스트/아이콘 및 오프라인 실행은 확인했습니다.
- 네이버 사전은 외부 서비스입니다. 검수 시점의 응답만 확인했으며 향후 접근 제한이나 구조 변경까지 보장하지 않습니다.
